import { CircularProgress } from "@mui/material";

const Loader = () => {
  return (
    <section className="loader container">
      <CircularProgress />
    </section>
  );
};

export default Loader;
